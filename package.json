{
  "name": "token-vesting",
  "version": "1.0.0",
  "description": "Token vesting project for Herita internship application",
  "type": "module",
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.4",
    "hardhat": "^3.4.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
